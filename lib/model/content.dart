import 'package:flutter/material.dart';

class Content {
  final String title;
  final Image image;
  final String text;
  final Color titleColor;

  Content(this.title, this.image, this.text, this.titleColor);
}